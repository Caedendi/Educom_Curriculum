{% extends 'base.html.twig' %}

{% block title %}Artiest{% endblock %}

{% block body %}
    <h1>Artiest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artiest.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ artiest.naam }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ artiest.genre }}</td>
            </tr>
            <tr>
                <th>Omschrijving</th>
                <td>{{ artiest.omschrijving }}</td>
            </tr>
            <tr>
                <th>Afbeelding_url</th>
                <td>{{ artiest.afbeeldingUrl }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ artiest.website }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artiest_index') }}">back to list</a>

    <a href="{{ path('artiest_edit', {'id': artiest.id}) }}">edit</a>

    {{ include('artiest/_delete_form.html.twig') }}
{% endblock %}
